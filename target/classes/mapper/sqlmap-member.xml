<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
     <!-- 로그인 -->
	 <select id="login" resultType="memberDTO" parameterType="String">
        select *
        from member
        where id = #{id}
   	  </select>
   	  
   	  <!-- 회원가입 -->
   	  <insert id="register"  parameterType="memberDTO">
		insert into member (num,id,pw,name,local,gender,nickname,email,admin)
		values(member_seq.nextval,#{id},#{pw},#{name},#{local},#{gender},#{nickname},#{email},0)
   	  	<selectKey keyProperty="num" resultType="Integer">
		<!--  NVL : 지정값이 null이면, 다음값으로 출력  -->
		select nvl(max(num), 0) from member
		</selectKey>
   	  </insert>
   	  
   	  <select id="idCheck" resultType="int">
	      select count(*) 
	      from member
	      where id = #{id}
	</select>
</mapper>